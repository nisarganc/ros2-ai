Fix the webcam name instead of number
Handle exception case where 0 marker is not detected
    - detect and estimate only once?
    - just publish x, y, yaw
Decide the poses pulishing rate

check if robot frame and aruco frame coincide with each other?
check if increading k results in smooth traj
Fix that centroid dont rotate while generating control commands